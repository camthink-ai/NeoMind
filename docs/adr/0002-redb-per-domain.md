# ADR-0002:redb 按领域分库

## 状态
生效(项目初始架构)

## 背景
边缘设备上运行,不能假设有外部数据库进程;同时要求数据可持久、崩溃安全。

## 决策
每个领域一个独立 redb 文件(telemetry/devices/agents/rules/…共 16 个),无 ORM、无嵌入 SQL。模式即 Rust 结构体 + bincode/serde。

## 后果
+ 零外部依赖、部署即拷贝;单库损坏不殃及其他领域;领域边界在存储层物理可见。
- 跨库无事务;复杂查询手写索引;schema 迁移靠手写版本逻辑。
