# ADR-0004:扩展双模隔离(原生进程 + WASM 沙箱)

## 状态
生效

## 背景
扩展市场需要运行第三方代码;既要性能(视觉推理)又要安全(不可信代码)。

## 决策
原生扩展(cdylib)跑在 `extension-runner` 独立进程,经 IPC 通信;WASM 扩展跑在 wasmtime,进程内但受三层资源限制(见 ADR-0018)。SDK 公开面 `neomind-extension-sdk` 尽量只增不减。

## 后果
+ 崩溃隔离(扩展挂了主服务活着);能力授权经宿主统一审计。
- IPC 序列化成本;调试链路长(宿主↔runner↔扩展)。
