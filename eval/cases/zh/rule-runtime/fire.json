{
  "id": "rule-fire-runtime",
  "lang": "zh",
  "category": "rule",
  "workflow": "rule-runtime",
  "scenario_type": "single_turn",
  "description": "运行时验证·规则触发执行(NeoMind 自动化核心链):agent 建规则(battery_level<20→notify),eval 通过 webhook 摄取路径注入触发性遥测(battery_level=10<20,webhook 发 DeviceMetric 事件触发规则引擎),验证规则真 fire 产生了告警消息。从'能配置'升级到'配了真管用'。新维度:runtime rule firing。",
  "setup": { "fixture": "scenario-rule-runtime" },
  "turns": [
    { "user": "建一条规则,名字叫 low-battery-alert:当 webhook-sensor 的 battery_level 低于 20 时,发一条告警通知。" }
  ],
  "applies": ["tool_accuracy", "task_completion", "language_adherence"],
  "expectations": {
    "per_turn": ["Turn 1: `neomind rule create` 建规则 low-battery-alert(condition: device:webhook-sensor:battery_level < 20, action: notify)"],
    "overall": "规则建成 + runtime webhook 注入 battery_level=10 后规则 fire 产生告警消息"
  },
  "expect": { "tools": ["shell"], "commands": ["neomind rule create"] },
  "state_queries": [
    { "type": "rule_exists", "params": { "name": "low-battery-alert" }, "expected": true }
  ],
  "runtime": {
    "trigger": [
      { "type": "webhook", "device_id": "webhook-sensor", "metric": "battery_level", "value": 10 }
    ],
    "wait_ms": 3000,
    "expect": [
      { "type": "message_count", "expected_min": 1 }
    ]
  }
}
