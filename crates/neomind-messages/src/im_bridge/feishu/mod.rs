//! Feishu (Lark) two-way IM bridge.
//!
//! Only the `pbbp2` wire codec is implemented here for now (Task 1 of the
//! Feishu bridge rollout). The WS client / `FeishuBridge` live in later tasks.

pub mod pbbp2;
